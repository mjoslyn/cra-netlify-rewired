{
  "name": "ps",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-loadable": "^5.5.0",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "pulp build && react-app-rewired start",
    "build": "pulp build && react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom"
  },
  "devDependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.7.3",
    "purs-loader": "^3.2.0",
    "react-app-rewired": "^1.6.2"
  }
}
